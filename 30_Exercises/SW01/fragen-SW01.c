Werden neue Files aus Terminal mit "touch name.format", oder mit anderem Befehl erstellt?
Wie wird der Texteditor aus der Terminal geöffnet?
Was macht der Befehl "pico name.format"?
Werden die verschiedenen Einzüge im Texteditor automatisch erstellt (wie im Bsp-C-Programm)?
Wann erkennt System ".c"-File als solches (Logo ändert im Verzeichnis)?
Weshalb muss mit "git config --global user.email" eine E-Mail-Adresse angegeben werden vor "git commit -m "..." "?
